# Repository Guidelines

- When changing card logic, effects, tags, descriptions, or other gameplay behavior for cards, update `doc/cards.md` in the same change so the documentation stays in sync with the code.
- Prefer keeping card docs and tests aligned with implementation changes.
